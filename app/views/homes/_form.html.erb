<%= form_with(model: home) do |form| %>
  <%= render "layouts/form_errors", object: form.object %>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
